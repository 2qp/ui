import ShellLayout from "@app/demo/[name]/blocks/shell-layout";
import { Component } from "@/components/rhf-select";

const rhfselect = {
  name: "rhf-select",
  components: {
    Default: (
      <ShellLayout>
        <Component />
      </ShellLayout>
    ),
  },
};

export { rhfselect };
